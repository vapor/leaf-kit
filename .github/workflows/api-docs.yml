name: deploy-api-docs
on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
     uses: vapor/api-docs/.github/workflows/build-and-deploy-docs-workflow.yml@68a8e1272af3e2a8dbf20bcadf86bab141afe9c2
     secrets: inherit
     with:
       package_name: leaf-kit
       modules: LeafKit
       pathsToInvalidate: /leafkit